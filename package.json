{
  "name": "ampcloud",
  "version": "1.0.0",
  "private": true,
  "description": "AmpCloud — Cloud-native Windows OS deployment platform",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
