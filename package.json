{
  "name": "nova",
  "version": "1.0.0",
  "private": true,
  "description": "Nova — Cloud-native Windows OS deployment platform",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "devDependencies": {
    "vite": "^6.3.5",
    "vitest": "^4.1.0"
  }
}
