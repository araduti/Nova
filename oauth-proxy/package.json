{
  "name": "nova-oauth-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Nova GitHub OAuth CORS Proxy — Cloudflare Worker",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250327.0",
    "typescript": "^6.0.2",
    "vitest": "^4.1.0",
    "wrangler": "^4.80.0"
  }
}
